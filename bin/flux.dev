#!/usr/bin/env node
const path = require('path');
const serverPath = path.resolve(__dirname, '../scripts/server.js');
require('child_process').execSync(`npx nodemon ${serverPath}`, { stdio: 'inherit' });
